language: python
python:
    - "2.7"
    - "3.4"
    - "3.5"
env:
    - DJANGO=1.9.1
    - DJANGO=1.8.8
install:
    - pip install -q Django==$DJANGO --use-mirrors
    - pip install -q -e . --use-mirrors
    - pip install -r requirements.txt
script:
    - python runtests.py
